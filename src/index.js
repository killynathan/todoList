import React from 'react';
import ReactDOM from 'react-dom';
var ToDoListContainer = require('./containers/toDoListContainer');

 
ReactDOM.render(
  <ToDoListContainer />,
  document.getElementById('root') 
);
